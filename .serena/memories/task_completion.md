# Task Completion

- For significant CSS/page edits, run `npm run build`.
- For Storytelling Lab visual edits, also check `/storytelling-lab` at desktop and mobile widths for overflow, text overlap, and section transition issues.
- For frontend visual changes, run a local dev server and visually verify the touched route(s) in browser when feasible.