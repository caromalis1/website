# Tech Stack

- React 18 with `react-scripts` 5 / Create React App.
- JavaScript components, plain CSS files imported per component/page; no TypeScript source despite project language detection.
- Dependencies include `gsap` for animation and `web-vitals`; dev dependencies include Playwright and Tailwind v4, though current app styling is plain CSS.
- Package manager artifacts: `package-lock.json`; use npm scripts.